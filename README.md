# CityPass_Login_Backend
To do:
- Deteccion de anomalias con IA --> Python
- Definir que devuelve el JWT
- TODO lo referido a registro --> Si es que corresponde
- Adaptacion para el bus del grupo 1

// al correrlo devuelve esto, supongo que habria que cambiarlo mas adelante pero por ahora está bien
: No se encontraron las claves RSA en file:./keys/private_key.pem / file:./keys/public_key.pem -- generando un par nuevo autom├íticamente. Esto es solo para DESARROLLO: en un ambiente real las claves deben administrarse como secrets del proveedor cloud, no autogenerarse en el filesystem.